[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "doi2proxy"
version = "0.1.2"
description = "Convert DOI to HKU EZproxy URLs - CLI tool"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Tan Zheng", email = "zhengtan@connect.hku.hk"}
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
doi2proxy = "doi2proxy.cli:main"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/doi2proxy"]
